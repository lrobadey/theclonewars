# TODO
- [x] Review existing entry points to outline how the Textual app and FastAPI server start.
- [x] Create a `clone` launcher script that runs both services together with clean shutdown handling.
- [x] Document the new `python3 clone` workflow alongside the existing run instructions.
- [x] Implement a launcher that only runs the FastAPI browser server.
- [x] Update the README text to describe the browser-only `python3 clone`.
